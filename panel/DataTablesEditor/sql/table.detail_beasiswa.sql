-- 
-- Editor SQL for DB table detail_beasiswa
-- Created by http://editor.datatables.net/generator
-- 

CREATE TABLE `detail_beasiswa` (
	`ID` int(10) NOT NULL auto_increment,
	`IDBEASISWA` varchar(255),
	`IDASPEK` varchar(255),
	`BOBOT` varchar(255),
	PRIMARY KEY( `ID` )
);